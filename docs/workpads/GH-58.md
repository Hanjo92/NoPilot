# GH-58 Workpad

- Updated: 2026-06-04 (continuation turn #20)
- Issue: `GH-58` `Set up weekday Slack notification for newly created issues`
- Intended tracker label: `Done` once GitHub sync is available

## Progress

- Added `.github/workflows/weekday-issue-slack-notifier.yml` for a weekday 09:00 `Asia/Seoul` run, plus manual `workflow_dispatch` support with an optional `lookback_hours` override.
- Added `.github/scripts/weekday-issue-slack-notifier.mjs`, a dependency-free Node script that finds non-PR issues created after the previous completed notifier run and posts a compact Slack message through an incoming webhook.
- Defaulted the Slack destination decision to a shared channel rather than a DM, because an incoming webhook is simpler to operate and keeps the scheduled alert visible to the team.
- Added maintainer-facing setup docs and local finish-helper artifacts so the GitHub workpad comment and tracker label can be synced as soon as GitHub auth is usable again.

## Validation

- `node --check .github/scripts/weekday-issue-slack-notifier.mjs` passed.
- `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs` passed, including source assertions for `.github/workflows/weekday-issue-slack-notifier.yml`.
- `bash GH-58-finish.test.sh` passed.
- `git diff --check` passed.
- `gh auth status` still fails in this session, so live GitHub label/comment mutation could not be validated.
- Revalidated on continuation turn `#2`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#2`: `./GH-58-finish.sh` still stops at the first `gh auth status` check, prints the blocked summary, and exits before any remote label or comment mutation.
- Revalidated on continuation turn `#3`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#3`: `gh issue view 58 --repo Hanjo92/NoPilot --json number,title,labels,state` now fails earlier with `error connecting to api.github.com`, so there is still no live GitHub read or mutation path from this session.
- Revalidated on continuation turn `#4`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#4`: `gh auth status`, `gh issue view 58 --repo Hanjo92/NoPilot --json number,title,labels,state`, and `./GH-58-finish.sh` all fail at the same external GitHub access boundary as before, before any remote label or comment mutation.
- Revalidated on continuation turn `#5`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#5`: `gh auth status`, `gh issue view 58 --repo Hanjo92/NoPilot --json number,title,labels,state`, and `./GH-58-finish.sh` still fail at the same external GitHub access boundary; the public web read path also still does not provide a dependable live issue view from this session.
- Revalidated on continuation turn `#6`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#6`: `gh auth status` still reports invalid tokens, `gh issue view 58 --repo Hanjo92/NoPilot --json number,title,labels,state` still fails with `error connecting to api.github.com`, and `./GH-58-finish.sh` still exits before any remote mutation.
- Revalidated on continuation turn `#7`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#7`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `gh auth status` still reports invalid tokens, and `./GH-58-finish.sh` still exits before any remote mutation step.
- Revalidated on continuation turn `#8`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#8`: `GITHUB_TOKEN` is still present in the environment, but `gh auth status` still reports that token as invalid, and `./GH-58-finish.sh` still exits before any remote mutation step.
- Revalidated on continuation turn `#9`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#9`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1` and `GITHUB_TOKEN_PRESENT=yes`, but `gh auth status` still reports the token as invalid and `./GH-58-finish.sh` still exits before any remote mutation step.
- Revalidated on continuation turn `#10`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#10`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1` and `GITHUB_TOKEN_PRESENT=yes`, `gh auth status` still reports the token as invalid, and `./GH-58-finish.sh` still exits before any remote mutation step.
- Revalidated on continuation turn `#11`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#11`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid and `./GH-58-finish.sh` still exits before any remote mutation step.
- Revalidated on continuation turn `#12`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#12`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#13`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#13`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#14`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#14`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#15`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#15`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#16`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#16`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#17`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#17`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#18`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#18`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#19`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#19`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#20`: `node --test .github/scripts/weekday-issue-slack-notifier.test.mjs`, `bash GH-58-finish.test.sh`, and `git diff --check` still pass with no local drift.
- Revalidated on continuation turn `#20`: this session still has `CODEX_SANDBOX_NETWORK_DISABLED=1`, `GITHUB_TOKEN_PRESENT=yes`, and `LINEAR_API_KEY_PRESENT=yes`, but `gh auth status` still reports the GitHub token as invalid, `./GH-58-finish.sh` still exits before any remote mutation step, and `linear_graphql` still returns `401`.

## Blockers

- `gh auth status` reports invalid GitHub credentials for both `GITHUB_TOKEN` and the default `gh` account.
- Because of that, I could not update the running GitHub workpad comment or move the issue label to `Done` on the tracker.
- Revalidated on continuation turn `#2`: the blocker is still external GitHub auth, not unfinished local implementation or validation work.
- Revalidated on continuation turn `#3`: `linear_graphql` also still fails with `401`, so there is no alternate authenticated tracker integration path available in-session.
- Revalidated on continuation turn `#4`: the blocker remains unchanged and external; no new in-session GitHub or alternate tracker sync path appeared.
- Revalidated on continuation turn `#5`: the blocker remains unchanged and external; no dependable GitHub CLI, public-web, or alternate tracker sync path appeared in-session.
- Revalidated on continuation turn `#6`: the blocker remains unchanged and external; there is still no in-session path to read or mutate the live GitHub issue state.
- Revalidated on continuation turn `#7`: the blocker remains unchanged and external; the session is still network-disabled in addition to having invalid GitHub credentials.
- Revalidated on continuation turn `#8`: the blocker remains unchanged and external; the session still has a present but unusable `GITHUB_TOKEN`, plus the same network-disabled constraint.
- Revalidated on continuation turn `#9`: the blocker remains unchanged and external; the session is still network-disabled and the present `GITHUB_TOKEN` is still unusable.
- Revalidated on continuation turn `#10`: the blocker remains unchanged and external; the session is still network-disabled and the present `GITHUB_TOKEN` is still unusable.
- Revalidated on continuation turn `#11`: the blocker remains unchanged and external; both GitHub and Linear credentials are present in the environment, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#12`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#13`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#14`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#15`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#16`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#17`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#18`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#19`: the blocker remains unchanged and external; both GitHub and Linear credentials are still present, but GitHub auth is still invalid and `linear_graphql` still returns `401`.
- Revalidated on continuation turn `#20`: this remains a true external blocker at the end of the agent run. The only incomplete actions are the GitHub `Done` label transition, the marker-tagged workpad comment sync, and any dependent tracker sync that requires valid remote auth.

## Remote Sync

- Workpad comment file: `GH-58-comment.md`
- Finish helper: `./GH-58-finish.sh`
- Local helper validation: `./GH-58-finish.test.sh`
