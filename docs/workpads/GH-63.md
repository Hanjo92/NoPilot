# GH-63 Workpad

- Updated: 2026-06-09
- Issue: `GH-63` `[Feature] Chat pannel`
- Intended tracker label: `Done` once GitHub sync is available

## Progress

- Added a persistent `Chat` webview under the existing NoPilot Activity Bar container so the extension now exposes a sidebar agent panel instead of only one-shot inline chat.
- Added the `NoPilot: Open Chat Panel` command and a matching Activity Bar menu shortcut for opening the chat view directly.
- Added panel-chat request support to the shared completion prompt builder, including multi-turn history, active file metadata, surrounding editor context, and selected-code context.
- Added assistant reply actions that can insert the latest response at the cursor or replace the current selection after stripping markdown code fences.
- Updated Marketplace and README copy so the extension description, highlights, and menu walkthrough mention the new chat panel workflow.
- Added an `Unreleased` changelog entry for the persistent chat panel feature.
- Added focused source tests for the chat panel prompt path, manifest wiring, extension activation wiring, and chat webview building blocks.
- Fixed the test harness wiring so the new chat-view sources and tests are included in `tsconfig.test.json` and the npm test runner list instead of being present only in `package.json`.
- Fixed chat-panel apply actions so mixed prose-plus-code responses now prefer the first fenced code block before falling back to plain fence stripping, avoiding explanation text being inserted into the editor.
- Added `GH-63-comment.md`, `GH-63-finish.sh`, and `GH-63-finish.test.sh` so the running workpad comment and `Done` label can be synced once GitHub access is restored.

## Validation

- `git diff --check` passed.
- `npm test` could not run to completion because `tsc` is not installed in this workspace (`sh: 1: tsc: not found`).
- A targeted Node-based validation script passed for the chat panel manifest entries, extension wiring, prompt-builder support, chat webview assets, README copy, and changelog entry.
- Revalidated on continuation turn `#2`: the chat-view test wiring validation passed after adding the new chat panel files to `tsconfig.test.json` and the npm test command, `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, and `bash GH-63-finish.sh` still stops at the first live GitHub auth step before any mutation.
- Revalidated on continuation turn `#3`: the chat apply-extraction validation passed after adding fenced-code extraction for assistant replies, `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, and `bash GH-63-finish.sh` still stops at the first live GitHub auth step before any mutation.
- Revalidated on continuation turn `#4`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, and both `bash GH-63-finish.sh` and `gh issue view 63 --repo Hanjo92/NoPilot --json number,title,labels,state` still fail before any remote mutation because GitHub auth/network access remain unusable in-session.
- Revalidated on continuation turn `#5`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#6`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#7`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#8`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#9`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#10`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#11`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#12`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#13`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#14`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#15`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#16`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#17`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#18`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#19`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- Revalidated on continuation turn `#20`: `git diff --check` still passes, `bash GH-63-finish.test.sh` still passes, `git status --short` still shows only the expected GH-63 implementation and handoff files in the worktree, and the live GitHub sync path still fails before any remote mutation.
- `bash GH-63-finish.test.sh` passed.
- `bash GH-63-finish.sh` fails immediately at `gh auth status` and prints the expected blocked GitHub summary without mutating labels or comments.

## Blockers

- `gh auth status` reports invalid GitHub credentials for both `GITHUB_TOKEN` and the default `gh` account.
- `gh issue view 63 --repo Hanjo92/NoPilot --json number,title,labels,state` fails with `error connecting to api.github.com`, so the tracker label and running workpad comment cannot be synced from this session.
- Revalidated on continuation turn `#2`: blocker status is unchanged; the prepared finish helper still exits at `gh auth status` before any GitHub label/comment mutation can occur.
- Revalidated on continuation turn `#3`: blocker status is still unchanged; the prepared finish helper still exits at `gh auth status` before any GitHub label/comment mutation can occur.
- Revalidated on continuation turn `#4`: blocker status is still unchanged; there is still no working in-session path to perform the required GitHub label/comment mutation or read the live issue label state.
- Revalidated on continuation turn `#5`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#6`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#7`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#8`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#9`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#10`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#11`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#12`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#13`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#14`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#15`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#16`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#17`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#18`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#19`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- Revalidated on continuation turn `#20`: blocker status is still unchanged; `gh auth status` still reports invalid GitHub credentials and `gh issue view` still fails with `error connecting to api.github.com`, so there is still no working in-session path to read or mutate the tracker remotely.
- End-of-run status: this remains a true external blocker rather than unfinished engineering work. The only incomplete actions are the GitHub label transition, the marker-tagged workpad comment sync, and any dependent tracker state update that must happen after those remote mutations.

## Remote Sync

- Workpad comment file: `GH-63-comment.md`
- Finish helper: `./GH-63-finish.sh`
- Local helper validation: `./GH-63-finish.test.sh`
